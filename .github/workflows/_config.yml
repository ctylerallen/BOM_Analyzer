# _config.yml (Example)

title: BOM Analyzer Showcase
description: Portfolio project for BOM analysis.
# theme: jekyll-theme-minimal   # Option 1: Use a basic supported theme
remote_theme: pages-themes/minimal@v0.2.0 # Option 2: Use remote theme (recommended)

plugins:
  - jekyll-remote-theme # Required if using remote_theme
  - jekyll-feed
# Add other Jekyll 
